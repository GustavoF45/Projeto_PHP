Para testar o sistema, adicione os dados necessários na página "conexao.php".

Necessário a criação do banco de dados "ies" e executar os comandos de "professores.sql" e "horarios.sql" para criação das tabelas.